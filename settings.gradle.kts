rootProject.name = "g2k"

pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

// Configure project's dependencies
dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
